services:
  - type: web
    name: fullstack-capstone-project
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node app.js
    envVars:
      - key: MONGO_URL
        sync: false
      - key: NODE_ENV
        value: production